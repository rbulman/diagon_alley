import {connect} from "react-redux";
import About from "APP/app/components/About";

const mapStateToProps = () => ({})
const mapDispatchToProps = () => ({})

export default connect(mapStateToProps, mapDispatchToProps)(About)